{
  "name": "game-calendar",
  "version": "1.0.0",
  "description": "Calendario uscite videogiochi auto-aggiornato",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "deploy": "npm run build && wrangler deploy",
    "preview": "npm run build && wrangler dev"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "wrangler": "^4.87.0"
  }
}
